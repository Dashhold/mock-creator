# Keep the build context small and the image free of local artefacts.
.git
.env
*.md
*.txt
Dockerfile*
.dockerignore

# Raw material is no longer read from disk; documents are uploaded through the API.
source/

# Local build output and editor noise
server
server.exe
*.exe
*.log
.idea/
.vscode/
